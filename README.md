Pré requisitos para compilar: g++(versão 9) e GNU MAKE
Caso possua uma versão superior do g++, favor editar o indicador do compilador no makefile (CC).

Acessar o diretório /search_engine/src/ e executar o make e executar o binário:

make
./engine
